import pandas as pd
import tensorflow as tf
from matplotlib import pyplot as plt

#context specific
		build_model(my_learning_rate) #which builds a randomly-initialized model.
		train_model(model, feature, label, epochs) # trains the model from the examples (feature and label) you pass.

