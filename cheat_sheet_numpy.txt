import numpy as np

np.array([...]) # creates an array
np.array([[...]...]) # creates a matrix
np.arange(5,11) #  creates an array from 5 to 10
np.random.randint(low = ?, high = ??, size = (6,) # random ints from ? to ??-1 of size 6
np.random.random((6,)) # random floatings in (0,1) of size 6
np.zeros(4) # creates array with 4 zeros
np.ones(4) 
